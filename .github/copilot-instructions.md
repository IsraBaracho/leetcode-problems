<!-- Arquivo criado automaticamente para instruções internas do workspace -->
- Verifique: `README.md` existe e contém instruções de uso.
- Estrutura esperada: `src/problems/<nome>.ts` com `export async function run()`.
- Para executar: `npm install` e `npm run start -- <problemName>`.
